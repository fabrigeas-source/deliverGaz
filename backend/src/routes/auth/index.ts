/**
 * Authentication Routes Module
 * 
 * This module exports all authentication-related routes
 * for the DeliverGaz application.
 */

// Export auth routes as default
export { default } from './auth.route';

// Re-export for named imports
export { default as authRoutes } from './auth.route';